include $(TOPDIR)/rules.mk

ARCH:=aarch64
BOARD:=ipq53xx
BOARDNAME:=Qualcomm IPQ53XX
FEATURES:=squashfs ramdisk ext4 display
CPU_TYPE:=cortex-a53
CPU_SUBTYPE:=generic
KERNEL_PATCHVER:=5.10

define Target/Description
    Build firmware images for Qualcomm IPQ53XX based boards.
endef

include $(INCLUDE_DIR)/target.mk
